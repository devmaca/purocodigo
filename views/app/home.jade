extends ../layout.jade
block contenido
	p Nombre de Usuario : #{user1.username}
	p Email : #{user1.email}
	p Id_Usaurio: #{user1.id}
	for imagen in imagenes
		div(class="img-container")
			h2=imagen.title
			img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img")
			p Por #{imagen.creator.email}

	